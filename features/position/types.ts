export interface Position {
  id: string;
  code: string;
  name: string;
  departmentCode: string;
  level: number;
  description: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
}